version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    ports:
      - "8000:8000"

  agent:
    build:
      context: .
      dockerfile: docker/Dockerfile.agent
    depends_on:
      - api
    environment:
      - API_URL=http://api:8000/metrics